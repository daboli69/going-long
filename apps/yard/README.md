Going Yard UI imported from daboli69/hr-board, baseline 2032ac4. The authoritative UI is docs/index.html and docs/going-platform.js in that repository. This checked-in copy is deployed at /yard/. Baseball model data stays live via api/yard-snapshot.mjs; no contact-model weights are changed here. Shared live prices use /api/odds?sport=mlb. Re-sync both files when updating the upstream UI.
